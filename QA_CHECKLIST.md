# QA Checklist — لوحة الإدارة (Admin)

1. إعداد البيئة
   - تأكد من تشغيل قاعدة البيانات وتطبيق ملف `sql/upgrade_admin.sql` لإنشاء الجداول المساعدة.
2. تسجيل الدخول
   - محاولة تسجيل الدخول باسم إداري صحيح — تأكد من نجاح الانتقال إلى `admin/index.php` ووجود CSRF token في الجلسة.
   - إدخال بيانات خاطئة 6 مرات وتأكد من حظر المحاولات مؤقتاً خلال 15 دقيقة.
3. إدارة المنتجات
   - إضافة منتج جديد مع صورة وفيديو: تحقق من حفظ المنتج، وجود السجل في `products` و`product_images` و`product_videos`، وتسجيل إدخال في `admin_audit_logs`.
   - تعديل منتج: تغيير العنوان أو الصورة وتحقق من تحديث السجلات وتسجيل التدقيق.
   - حذف منتج: تحقق من حذف السجل وتسجيل التدقيق.
4. إدارة التصنيفات
   - إضافة تصنيف جديد وتحقق من السجل وتسجيل التدقيق.
   - حذف تصنيف وتحقق من السجل وتسجيل التدقيق.
5. إدارة المستخدمين
   - تبديل صلاحية مستخدم وتحقق من تحديث `role_id` وتسجيل التدقيق.
6. تغيير كلمة المرور
   - تغيير كلمة المرور للمشرف والتحقق من نجاح العملية وتسجيل التدقيق.
7. رفع الوسائط
   - رفع صورة أكبر من 5MB — يجب أن يفشل.
   - رفع ملف بخلاف الصور (مثلاً php) إلى `uploads/` — يجب أن يتم منعه عبر `.htaccess`/`web.config`.
8. CSRF وSession
   - تأكد من إرسال رأس `X-CSRF-Token` في طلبات AJAX المؤثرة.
   - التحقق من أن `session.cookie_httponly` و`SameSite` مفعلان عبر استعراض الكوكيز.
9. سجلات وتدقيق
   - تحقق من محتوى `admin_audit_logs` بعد تنفيذ العمليات (action, details, admin_id, ip).

أضف أي خطوات اختبار إضافية حسب تدفقات عملك.
